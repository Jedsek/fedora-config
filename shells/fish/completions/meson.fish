complete -x -c mi -a "vala" -d "Init meson.build for vala"
complete -x -c mi -s l -l list -d "List supported languages"
complete -x -c mi -s h -l help -d "Show helps"

complete -x -c m -a "b" -d "Meson build"
complete -x -c m -a "r" -d "Meson rebuild"